@model project_ead.Models.NewsAndComment

@{
    Layout = "~/Views/Shared/LayoutPg1.cshtml";
    }
<div id="carousel-example-generic" class="carousel slide" data-ride="carousel">
  <!-- Indicators -->
  
 
  <!-- Wrapper for slides -->
  <div class="carousel-inner">
    <div class="item active">
      <img src="~/img/banner.jpg" alt="img" width="1500">
      <div class="carousel-caption">
          <h2>upload news </h2>
      </div>
    </div>
    
    </div>
        </div>
          <!-- End course content -->



<br/><br/><br/>

  <!-- End course content -->
<div>
                           <div class="form-group" hidden>
                                
                               @Html.TextBoxFor(m=>m.Id,new { @class="form-control input-lg" })
                            </div>
                             <div class="form-group">
                                
                                    @Html.LabelFor(m=>m.type,Model.type, new {@class="form-control input-lg"})                          

                             </div>
                            <div class="form-group">
                               @Html.LabelFor(m=>m.detail,Model.detail,new { @class="form-control input-lg" })

                            </div>
                            <div class="form-group">
                                <label class="form-control input-lg"> @Model.date</label>

                            </div>
                            <br />
                            <br />
                             @foreach(var y in Model.Lc)
                            {
                             <div class="form-group">
                               @Html.LabelFor(m=>m.type,y.comment1,new { @class="form-control input-lg"  })

                            </div>
                            }
                            <form class="form col-md-12 center-block" method="post" action="/Home/save_comment" >


                            <div class="form-group">
                              
                                <input type="text" name="cmt" class="form-control input-lg" id ="cmt" />

                            </div>
                        <div class="form-group" >
                                <input type="hidden" value="@Model.Id" name="idP" id="idP" />
 
                            </div>
                           
                            <div class="form-group" >
                                <input type="submit" value="komment !!"  class="btn btn-primary btn-lg btn-block" />
 
                            </div>
                        </form>

                    </div>
